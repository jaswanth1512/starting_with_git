->  git clone "URL"
	then all the files will be downloaded into local PC

-> git status
	then we will see the status of the changes made in our file


// after making desired changes then we should add them to repo to do that follow these steps

-> git add "filename"
	This will add all the files

-> git commit -m "message"
	This will commit the status using a message 

-> git log
	This will show the status of our log

-> git push
	This will push the code into github

-> git branch -a 
	This will show all the branches of that repo

-> git commit -b "new branch name"
	This will create a new branch in local repo

-> git push origin "new branch name"
	This will push the new branch to github
